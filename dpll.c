/*
 * dpll.c
 *
 * Placeholder for a DPLL-based model counting implementation. The project
 * currently uses brute-force enumeration; a DPLL implementation would be
 * required to scale beyond the 64-bit mask limit.
 */

#include "header.h"

/* Stub for DPLL-based counting. Returns 0 until implemented. */
int run_dpll_counting(Formula *f, int procs) {
  (void)f;
  (void)procs;
  return 0;
}

/*
    TODO: create benchmark with sequential and parallel implementationf for the OMICS Cluster

    1. finish sequential code
    2. finish parallel code
    3. finish benchmark
    4. edit makefile

    Idea:
        - use the already provided benchmark files as template
        - no need for a generator as we have pregenerated test cases
            - add switch to generate new test cases if possible (requires the omics cluster to have python)
*/